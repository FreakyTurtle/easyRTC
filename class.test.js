import { Bond } from './index';

test('Test Constructor', () => {
    // let bond = new Bond({}, 'test123', (msg, to = null) => {
    //     expect(msg).toBeTruthy();
    // });
    // expect(bond.getLocalStream()).tobe({});
})
